// Content script that will run on web pages

// This empty export makes this a module
export {};

document.addEventListener('DOMContentLoaded', () => {
    console.log('Enchir content script loaded');
    // Add your content script logic here
});
